#!/usr/bin/env node

console.log(`
    react-native-security-shield installed.
    
    Next steps:
    1. Create securityshield.config.json in your app root
    2. Run: npm run generate-native-config
    3. Run: npm run encode-pins
    4. Rebuild your app
    `);